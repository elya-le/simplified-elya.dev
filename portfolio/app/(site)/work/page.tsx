import PageShell from "@/components/PageShell";

export default function WorkPage() {
  return (
    <main>
      <PageShell>
        <section className="pt-10 sm:pt-14 pb-16 sm:pb-24">
          <h1 className="text-2xl sm:text-3xl tracking-tight">Work</h1>
          <p className="pt-6 max-w-[60ch] opacity-80">
            Placeholder. Next we’ll build this to match the Work page layout and
            interactions.
          </p>
        </section>
      </PageShell>
    </main>
  );
}
